# The following is the backend implementation for our diagnosis project.
# This porgram allows us to host a simple API for our machine learning model.





# The model can be activated using:
uvicorn main:app --reload
